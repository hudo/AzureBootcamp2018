{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/BootShop.Service.Downstream/BootShop.Service.Downstream.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}